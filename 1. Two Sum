class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        x = {}

        for position, number in enumerate(nums):
            y = target - number
            
            if y in x:
                return [x[y], position]
                
            x[number] = position
            
        return []

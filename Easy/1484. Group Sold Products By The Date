import pandas as pd

def categorize_products(activities: pd.DataFrame) -> pd.DataFrame:
    x = activities.groupby("sell_date",as_index=False)["product"].agg(
        num_sold = "nunique" ,
        products = lambda r : ",".join(sorted(r.unique()))
    ).sort_values("sell_date")
    return x

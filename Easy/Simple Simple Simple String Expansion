import re
def string_expansion(s):
    res=[]
    x = re.findall(r"(\d?)([a-zA-Z]+)",s)
    for n,c in x:
        n = int(n or 1)
        res.append("".join(n*char for char in c)) 
    return "".join(res)

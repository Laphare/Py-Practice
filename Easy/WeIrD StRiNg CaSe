def to_weird_case(words):
    x = words.split(" ")
    r = []
    for word in x:
        new_word = "".join(word[i].upper() if i %2 ==0 else word[i].lower() for  i in range(len(word)))
        r.append(new_word)
    return " ".join(r)

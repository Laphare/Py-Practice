def get_length_of_missing_array(array_of_arrays):
    if len(array_of_arrays) == 0 or None in array_of_arrays or [] in array_of_arrays:
        return 0
    x=[]

    for y in array_of_arrays:
        x.append(len(y))
        
    i = min(x)
    while i in x:
        i += 1
    return i
